import { forEachTreeOption, getTreeOptionsTillLevel, getValuesFromOptions, getValuesFromTreeOptions } from './options';

const makeOptions = () => [
  {
    key: '1',
    label: '1',
    value: '1',
  },
  {
    key: '2',
    label: '2',
    value: '2',
  },
];

// TODO: write me
describe.skip('utils.common.options', () => {
  describe('forEachTreeOption', () => {
    it('test', () => {
      const result = true;
      expect(result).toBe(true);
    });
  });
});
